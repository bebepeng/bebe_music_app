<!DOCTYPE HTML>
<html>
<head>
  <link href='http://fonts.googleapis.com/css?family=Dosis:400,600' rel='stylesheet' type='text/css'>
  <link href="<%= url('css/stylesheet.css') %>" rel="stylesheet" type="text/css"/>
  <title>New Song Form</title>
</head>
<body>
<div class="songlist">
  <form action="/home" method="post">
    <div class="form">
      <table>
        <tr>
          <td>Title:</td>
          <td><input type="text" name="title" placeholder="Title of Song" value="<%= session[:title] %>"></td>
          <% session.delete(:title) %>
        </tr>
        <tr>
          <td>Artist:</td>
          <td><input type="text" name="artist" placeholder="Artist of Song" value="<%= session[:artist] %>"></td>
          <% session.delete(:artist) %>
        </tr>
        <tr>
          <td>Album:</td>
          <td><input type="text" name="album" placeholder="Album of Song" value="<%= session[:album] %>"></td>
          <% session.delete(:album) %>
        </tr>
        <tr>
          <td>Language:</td>
          <td><input type="text" name="language" placeholder="Language the song is in" value="<%= session[:language] %>"></td>
          <% session.delete(:language) %>
        </tr>
        <tr>
          <td>Youtube:</td>
          <td><input type="text" name="youtube" placeholder="Link to Youtube" value="<%= session[:youtube] %>"></td>
          <% session.delete(:language) %>
        </tr>
      </table>
      <p class="error_message"><%= session[:you_error] %></p>
      <% session.delete(:you_error) %>
    </div>
    <input type="submit" value="Submit">
  </form>
</div>
</body>
</html>